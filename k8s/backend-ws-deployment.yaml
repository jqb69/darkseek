---
# k8s/backend-ws-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: darkseek-backend-ws
spec:
  replicas: 1
  selector:
    matchLabels:
      app: darkseek-backend-ws
  template:
    metadata:
      labels:
        app: darkseek-backend-ws
    spec:
      containers:
      - name: backend-ws
        image: gcr.io/PROJECT_ID_PLACEHOLDER/darkseek-backend-ws:latest
        imagePullPolicy: Always
        ports:
          - containerPort: 8000
        envFrom:
          - configMapRef:
              name: darkseek-config
          - secretRef:
              name: darkseek-secrets
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
