---
# k8s/frontend-service.yaml — FINNAL F*king VERSION
apiVersion: v1
kind: Service
metadata:
  name: darkseek-frontend
  namespace: default
spec: # NO annotations needed
  type: LoadBalancer
  selector:
    app: darkseek-frontend
  ports:
    # Service Port 80 (standard HTTP access) maps to the container's 8501 port
    - port: 80
      targetPort: 8501
      protocol: TCP
