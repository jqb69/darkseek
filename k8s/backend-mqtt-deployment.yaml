---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: darkseek-backend-mqtt
spec:
  replicas: 1
  selector:
    matchLabels:
      app: darkseek-backend-mqtt
  template:
    metadata:
      labels:
        app: darkseek-backend-mqtt
    spec:
      restartPolicy: Always  # Setting the restart policy to Always
      containers:
      - name: backend-mqtt
        image: us-central1-docker.pkg.dev/${GCP_PROJECT_ID}/darkseek/darkseek-backend-mqtt:latest
        envFrom:
        - configMapRef:
            name: darkseek-config
        - secretRef:
            name: darkseek-secrets
        livenessProbe:
          exec:
            command: ["test", "-f", "/tmp/mqtt-healthy"]  # Check if the health file exists
          initialDelaySeconds: 120                             # Delay before starting liveness checks
          periodSeconds: 40                                     # Frequency of liveness checks
          timeoutSeconds: 5
          failureThreshold: 3                                  # Number of failures before marking the container as unhealthy
        readinessProbe:
          exec:
            command: ["test", "-f", "/tmp/mqtt-healthy"]  # Check if the health file exists
          initialDelaySeconds: 60                             # Delay before starting liveness checks
          periodSeconds: 20                                     # Frequency of liveness checks
          timeoutSeconds: 5
          failureThreshold: 3                                  # Number of failures before marking the container as unhealthy
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
